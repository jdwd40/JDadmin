{
  "name": "jdadmin",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Admin dashboard for Coins and Dwarf Coins",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server & npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "test": "npm run test --workspace server && npm run test --workspace web",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "lint": "npm run lint --workspace server && npm run lint --workspace web",
    "migrate": "npm run migrate --workspace server",
    "create-admin": "npm run create-admin --workspace server",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "typescript-eslint": "^8.67.0"
  }
}
